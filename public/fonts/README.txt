فونت Vazirmatn برای اجرا روی شبکه داخلی (بدون اینترنت).
فایل اصلی: Vazirmatn-wght.woff2 (متغیر، وزن 100–900)

در index.css با @font-face بارگذاری می‌شود.
با npm run build داخل dist/fonts/ کپی می‌شود.
